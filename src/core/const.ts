/**
 * The name of the transition that will accept any non-error transition.
 */
export const WILDCARD_TRANSITION = '';
/**
 * The character that begins an error transition name.
 */
export const ERROR_PREFIX = '~';
/**
 * The character that separates depth in error transition names.
 */
export const ERROR_SPLIT = '.';